# Local Model Fixtures

This directory is reserved for local manual verification models, such as private
STEP or BREP files used while testing `osi-inspect`.

Do not commit CAD models from this directory. The repository ignores STEP, STP,
BREP, and RLE files here by default.
